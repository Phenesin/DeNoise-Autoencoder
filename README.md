# An implementation of denoising autoencoder implemented on MNIST digits dataset, which is of the dimension 28 * 28.

## It compresses the 28 * 28 image to 128 and vice versa, it reduces the dimensions and then reshape it to see how close the machine can redraw and classify

